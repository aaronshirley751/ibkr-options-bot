# Phase 3 Extended Dry-Run Configuration - 4+ Hours
broker:
  host: "192.168.7.205"
  port: 4001
  client_id: 254  # New clientId for Phase 3 restart
  read_only: false

paper_trading: true
dry_run: true

symbols: ["SPY", "QQQ", "AAPL"]

schedule:
  interval_seconds: 180  # 3-minute cycles for extended testing
  max_concurrent_symbols: 1  # Sequential processing to avoid clientId conflicts

risk:
  max_daily_loss_pct: 0.5  
  max_risk_per_trade_pct: 0.2  
  take_profit_pct: 0.5
  stop_loss_pct: 0.2

options:
  expiry: "weekly"
  moneyness: "atm"
  max_spread_pct: 2.0
  min_volume: 0
  strike_count: 3

monitoring:
  alerts_enabled: false
  heartbeat_url: ""

logging:
  level: "INFO"
