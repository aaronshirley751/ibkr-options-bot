# Phase 2 Test Configuration - Multi-Symbol
broker:
  host: "192.168.7.205"
  port: 4001
  client_id: 251  # New client ID for fresh Gateway connection
  read_only: false

paper_trading: true
dry_run: true

symbols: ["SPY", "QQQ", "AAPL"]

schedule:
  interval_seconds: 30  # 30 second cycles
  max_concurrent_symbols: 2  # Process 2 symbols in parallel

risk:
  max_daily_loss_pct: 0.5  
  max_risk_per_trade_pct: 0.2  
  take_profit_pct: 0.5
  stop_loss_pct: 0.2

options:
  expiry: "weekly"
  moneyness: "atm"
  max_spread_pct: 2.0
  min_volume: 0
  strike_count: 3

monitoring:
  alerts_enabled: false
  heartbeat_url: ""

logging:
  level: "INFO"
