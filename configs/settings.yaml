# Phase 1 Test Configuration - Single Symbol
broker:
  host: "192.168.7.205"
  port: 4001
  client_id: 250  # Fresh client ID for clean Gateway state
  read_only: false

paper_trading: true
dry_run: true

symbols: ["SPY"]

schedule:
  interval_seconds: 30  # Shorter cycle for testing
  max_concurrent_symbols: 1

risk:
  max_daily_loss_pct: 0.5  # 0.5% daily loss limit
  max_risk_per_trade_pct: 0.2  # 0.2% per trade
  take_profit_pct: 0.5
  stop_loss_pct: 0.2

options:
  expiry: "weekly"
  moneyness: "atm"
  max_spread_pct: 2.0
  min_volume: 0
  strike_count: 3

monitoring:
  alerts_enabled: false
  heartbeat_url: ""

logging:
  level: "INFO"
